import styled from 'styled-components';
import PropTypes from 'prop-types';

const PostList = React.memo(({}) => <StyledPostList></StyledPostList>);

const StyledPostList = styled.ol``;

PostList.propTypes = {};

export default PostList;
