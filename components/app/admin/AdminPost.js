import styled from 'styled-components';
import PropTypes from 'prop-types';

const AdminPost = React.memo(({}) => <StyledAdminPost></StyledAdminPost>);

const StyledAdminPost = styled.div``;

AdminPost.propTypes = {};

export default AdminPost;
